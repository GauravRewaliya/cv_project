<%= form_with(model: domain , data: { remote: true } ) do |form| %>
      <div class="form-group">
          <%= form.label :title %>
          <%= form.text_field :title ,class:"form-control" , required: true%>
      </div>

      <div>
        <%= form.submit class: 'btn btn-success' %>
      </div>
  <% end %>